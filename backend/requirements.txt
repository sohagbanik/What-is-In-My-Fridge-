# ============================================================================
# Python Backend Dependencies for "What's In My Fridge?"
# ============================================================================
#
# Install all dependencies with:  pip install -r requirements.txt
#
# Each package and its purpose:
# ============================================================================

# --- Web Framework ---
fastapi>=0.115.0          # Modern async web framework with auto-generated docs
uvicorn[standard]>=0.30.0 # ASGI server to run FastAPI (the [standard] extra includes uvloop for speed)

# --- AI / LLM ---
groq>=0.9.0               # Official Groq SDK for accessing LLaMA, Qwen vision models

# --- Image Processing ---
opencv-python>=4.10.0     # OpenCV — resize, contrast enhancement, JPEG encoding
numpy>=1.26.0             # NumPy — required by OpenCV for image arrays (np.ndarray)

# --- Data Validation ---
pydantic>=2.0.0           # Data validation (bundled with FastAPI, listed for clarity)

# --- Configuration ---
python-dotenv>=1.0.0      # Load environment variables from .env files

# --- WebSocket Support ---
websockets>=12.0          # WebSocket protocol support for live camera streaming

# --- File Upload Support ---
python-multipart>=0.0.9   # Required by FastAPI to handle file uploads (UploadFile)
